/* =========================================================
   DEVICELOG - SPLASH SCREEN
   ========================================================= */

// Wait until the page is completely loaded
document.addEventListener("DOMContentLoaded", function () {

    console.log("DeviceLog Splash Screen Loaded.");

});